type Account @entity {
  "Account address"
  id: ID!
}

type NameRegistered @entity {
  id: ID!
  blockNumber: Int!
  timestamp: DateTime!
  txHash: String!
  name: String!
  label: Bytes!
  owner: Account!
  expires: BigInt!
}
